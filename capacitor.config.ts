import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ng-lazy-load-hook-bug',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
